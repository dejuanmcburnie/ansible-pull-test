---
- name: Basic ansible-pull test
  hosts: localhost
  connection: local
  gather_facts: true

  tasks:
    - name: Create test file
      copy:
        dest: /tmp/ansible-pull-worked.txt
        content: |
          ansible-pull worked
          hostname={{ ansible_hostname }}
          date={{ ansible_date_time.iso8601 }}

    - name: Show success message
      debug:
        msg: "ansible-pull test completed"
